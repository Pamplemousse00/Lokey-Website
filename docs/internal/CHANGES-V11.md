# V11 changes

- Fixed the homepage header on compact desktop / Fold-style landscape viewports.
- Prevented desktop navigation labels from wrapping onto multiple lines.
- Between 931px and 1100px, the homepage hides the FAQ header shortcut and tightens spacing so the remaining navigation matches the product-page header density.
- Between 821px and 930px, only the homepage switches early to the compact Buy Lo-Key + hamburger layout; the product page retains its existing desktop header at the same widths because it already fits.
- Bumped the shared stylesheet cache version to v11.
