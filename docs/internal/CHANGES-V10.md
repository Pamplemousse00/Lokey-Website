# V10 changes

- Admin vehicle editor can apply a battery/status change across a model-year range.
- Range edits update existing rows and create missing years for the same make/model.
- A confirmation is shown for multi-year edits.
- Range edits are recorded as one audit action with counts of updated and newly created rows.
- No D1 schema migration is required.
