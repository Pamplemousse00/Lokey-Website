/*
 * Set this to the HTTPS endpoint that accepts new-vehicle requests.
 * The form sends JSON using POST with: year, make, model, source,
 * submittedAt, and pageUrl.
 */
window.LO_KEY_VEHICLE_REQUEST_API = '/api/vehicle-requests';
